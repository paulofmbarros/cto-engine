name: Plan Execute (Example)

on:
  push:
    paths:
      - 'projects/**/plan.yaml'
      - 'projects/**/.cto-engine/challenge-log.yaml'

jobs:
  validate-and-execute:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Build
        run: dotnet build CtoEngine.slnx
      - name: Validate
        run: dotnet run --project src/Cto.Cli -- validate --project ./projects/my-product --target all
      - name: Execute
        run: dotnet run --project src/Cto.Cli -- execute --project ./projects/my-product
        env:
          JIRA_EMAIL: ${{ secrets.JIRA_EMAIL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
